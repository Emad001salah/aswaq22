/**
 * src/constants/sample-posts.ts
 *
 * Production Social Feed Posts Module
 *
 * Empty array by default for production launch (loaded dynamically from database).
 */

export const DEFAULT_SOCIAL_POSTS = [];
