apiVersion: v1
kind: Service
metadata:
  name: {{ include "aswaq.fullname" . }}
  labels:
    {{- include "aswaq.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "aswaq.selectorLabels" . | nindent 4 }}
